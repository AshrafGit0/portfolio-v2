import LogoA from "../Svg/LogoA";

const HeaderLogo = () => {
  return (
    <div className="text-2xl flex font-bold text-gradient">
      <LogoA height={30} width={30} />
      shraf.dev
    </div>
  );
};

export default HeaderLogo;
